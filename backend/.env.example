# Backend Server Configuration
PORT=3001

# Database Configuration
# Option 1: Use DATABASE_URL (recommended for production)
# DATABASE_URL=postgresql://user:password@host:port/database

# Option 2: Use individual variables (for development)
DB_HOST=localhost
DB_PORT=5432
DB_USER=friday_user
DB_PASSWORD=friday_password
DB_NAME=friday_picker

# PostgreSQL Configuration (for Docker Compose)
# These are used by docker-compose.yml to configure the PostgreSQL container
POSTGRES_USER=friday_user
POSTGRES_PASSWORD=friday_password
POSTGRES_DB=friday_picker
