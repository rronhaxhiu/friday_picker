# Frontend API Configuration

# Backend URL for Vite dev server proxy
# Defaults to http://localhost:3001 if not set
VITE_BACKEND_URL=http://localhost:3001

# Full backend API URL for production builds
# Leave empty to use /api (for nginx proxy scenarios in Docker)
# Set this if you're building for production without a proxy
# VITE_API_URL=http://localhost:3001
